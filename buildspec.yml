version: 0.2

env:
  variables:
    PYTHON_VERSION: "3.13"

phases:
  install:
    commands:
      - curl -LsSf https://astral.sh/uv/install.sh | sh
      - export PATH="$HOME/.local/bin:$PATH"
      - echo "Setting up Python $PYTHON_VERSION using uv..."
      - uv python install $PYTHON_VERSION

  pre_build:
    commands:
      - echo "Installing dependencies with uv sync..."
      - uv sync

  build:
    commands:
      - echo "Checking formatting with uv run ruff format --check ..."
      - uv run ruff format --check .
      - echo "Running lint with uv run ruff check ..."
      - uv run ruff check .
      - echo "Running dead code analysis with uv run vulture ..."
      - uv run vulture src
      - echo "Running tests with uv run pytest for Python $PYTHON_VERSION..."
      - uv run pytest

cache:
  paths:
    - '~/.cache/uv/**/*'
